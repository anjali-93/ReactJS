// Question. Create a layout component <Children> that wraps children using props.children.

function Children(props){
    return (
        <>
        {props.children}
        
        </>
    );

}
export default Children;